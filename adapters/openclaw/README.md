# OpenClaw

Wire it:

```bash
brain onboard --agents openclaw --yes
```

Writes:

- `~/.openclaw/workspace/BRAIN.md`

Verify in OpenClaw:

```text
save a brain note: testing OpenClaw memory
what does brain remember about OpenClaw?
```
