{
  "$schema": "https://json.schemastore.org/claude-code-mcp-servers.json",
  "mcpServers": {
    "brain": {
      "command": "brain",
      "args": ["serve", "--mcp"],
      "env": {
        "BRAIN_DIR": "${HOME}/.brain"
      }
    }
  }
}
