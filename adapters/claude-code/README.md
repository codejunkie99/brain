# Claude Code

Wire it:

```bash
brain onboard --agents claude-code --yes
```

Writes:

- `~/.claude/mcp_servers.json`
- `~/.claude/CLAUDE.md`

Verify in Claude Code:

```text
use brain.ping
use brain.note to remember: "testing Claude Code memory"
use brain.ask to search for: "Claude Code"
```
