# Cursor

Wire it from your project:

```bash
brain onboard --agents cursor --yes
```

Writes:

- `.cursor/mcp.json`
- `.cursor/rules/brain.mdc`

Verify in Cursor:

```text
use brain.ping
use brain.note to remember: "testing Cursor memory"
use brain.ask to search for: "Cursor"
```
