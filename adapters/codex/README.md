# Codex

Wire it:

```bash
brain onboard --agents codex --yes
```

Writes:

- `~/.codex/config.toml`
- `~/.codex/AGENTS.md`

Verify in Codex:

```text
use brain.ping
use brain.note to remember: "testing Codex memory"
use brain.ask to search for: "Codex"
```
